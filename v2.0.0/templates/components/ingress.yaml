apiVersion: extensions/v1beta1
kind: Ingress
metadata:
  name: application-ingress
spec:
  rules:
  - host: {{ .Release.Namespace }}.k8s.metricstream.com
    https:
      paths:
      - backend:
          serviceName: tomcat-ssl-service
          servicePort: 443
