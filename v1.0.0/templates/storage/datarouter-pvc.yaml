apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: datarouter-pvc
  annotations:
    volume.beta.kubernetes.io/storage-class: ms-storage-class
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 1Gi
